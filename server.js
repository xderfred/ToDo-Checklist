var express = require('express');
var app = express();

var express = require('express');
var app = express();

const sqlite = require('./database.js')

const PORT = 3000;


app.listen(PORT);
console.log(`Server running on port ${PORT}`);